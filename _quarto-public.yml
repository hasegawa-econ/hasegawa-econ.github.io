# 公開プロファイル（デフォルト）: ダッシュボードを一切含めない
# quarto render / quarto publish gh-pages はこちらが使われる

project:
  render:
    - "**/*.qmd"
    - "!dashboard/"
    - "!kokusou/"
  resources:
    - "!dashboard/"
    - "!kokusou/"
